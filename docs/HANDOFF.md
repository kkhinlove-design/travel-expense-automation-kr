# 작업 인수인계

- 마지막 작업: 2026. 08. 19. 복명서 결재자 선택 목록에 `국장`을 추가해 환경설정에서 `국장 → 원장` 조합을 저장·출력할 수 있게 했으며, 기본 결재선 `팀장 → 실장`은 유지하고 80개 테스트와 프로덕션 빌드를 통과
- 다음 할 일: Vercel 운영 환경설정에서 1차 결재자 `국장`, 최종 결재자 `원장`이 표시·저장되는지 데스크톱·모바일 확인
- 주의사항: 운영 Vercel에는 `NEXT_PUBLIC_REPORT_APPROVER_TITLES` override가 없으므로 코드 기본 목록이 적용된다. 기존 직원별 저장 결재선은 변경하지 않으며 공유 Supabase 전체 `db push`는 실행 금지
