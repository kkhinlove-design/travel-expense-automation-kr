# 작업 인수인계

- 마지막 작업: 2026. 08. 18. 로그인 사용자별 기본 출발 사무소 환경 설정과 `travel_user_preferences` 소유권 RLS를 추가
- 다음 할 일: 운영 로그인으로 기본 사무소 저장 후 새 출장 자동 선택과 11개 기준지별 저장 운임 매칭을 확인
- 주의사항: 기본 사무소는 새 출장에만 적용하고 승인서에 실제 출발지가 있으면 원문을 우선함. 금액은 `wonAmount()`로 읽고 `pdfjs-dist` 변경 시 `npm run sync:pdf-worker` 실행
