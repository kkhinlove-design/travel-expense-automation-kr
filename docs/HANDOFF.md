# 작업 인수인계

- 마지막 작업: 2026. 08. 18. 전북 11개 사무소 출발 기준지 선택과 선택 전 여비·복명 단계 차단을 추가
- 다음 할 일: 배포 설정의 `ALLOWED_ORIGINS`·Secure password change를 확인하고 실제 저장 운임표에서 11개 기준지별 자동 매칭 결과 검증
- 주의사항: 정읍·익산·순창은 기본 기준지에서 제외하고 기존 저장값은 유지함. 금액은 `wonAmount()`로 읽고 `pdfjs-dist` 변경 시 `npm run sync:pdf-worker` 실행
