# 보안 정책

## 지원 범위

보안 수정은 기본 브랜치의 최신 버전을 대상으로 제공합니다. 공개 배포자는 최신 `main`과 lockfile을 기준으로 재배포해 주세요.

## 취약점 신고

인증 우회, 다른 사용자의 출장 열람, 원본 파일 접근, 관리자 권한 상승, 비밀키 노출 가능성은 공개 이슈에 적지 마세요.

1. GitHub 저장소의 **Security → Advisories → Report a vulnerability**에서 비공개 신고를 작성합니다.
2. 영향받는 경로와 버전, 최소 재현 절차, 예상 영향, 가능한 완화책을 포함합니다.
3. 실제 개인정보·비밀번호·토큰은 삭제하거나 가린 뒤 첨부합니다.

비공개 신고 기능이 보이지 않으면 상세 내용을 공개하지 말고, 최소 정보만 담은 이슈로 유지관리자에게 비공개 연락 경로를 요청하세요.

## 운영자 체크리스트

- Supabase publishable key만 `NEXT_PUBLIC_` 환경변수에 넣고 secret/service role key는 브라우저에 노출하지 않습니다.
- 모든 공개 스키마 테이블에 RLS와 사용자 소유권 정책을 적용합니다.
- `travel-sources` 버킷은 비공개로 유지하고 사용자별 경로 정책을 적용합니다.
- 관리자 이메일과 Edge Function secrets를 기관별로 교체합니다.
- Vercel Preview와 Production의 환경변수 및 데이터 프로젝트 분리를 검토합니다.
- 문서 원본의 보존기간을 정하고 만료된 데이터와 계정을 삭제합니다.
- Ollama의 11434 포트를 인터넷이나 사내 전체망에 공개하지 않습니다.

## 공개 시점

유지관리자는 신고를 확인한 뒤 영향과 우선순위를 평가하고, 수정과 배포가 준비되면 신고자와 공개 시점을 조율합니다. 즉시 차단이 필요한 경우 환경변수 회전, 기능 비활성화 또는 이전 배포로의 롤백을 먼저 수행할 수 있습니다.
